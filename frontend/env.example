# Frontend Port (for development)
PORT=3000

# Backend API URL (build-time only)
# Must use REACT_APP_ prefix for React to include it in the build
# Leave empty or unset to use relative URLs (same origin)
REACT_APP_API_BASE_URL=http://localhost:8000
